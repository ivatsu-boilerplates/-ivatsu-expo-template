import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headingText: {
    margin: 12,
    fontSize: 20,
    fontWeight: 'bold',
    textAlign: 'center',
  },
})

export default styles
